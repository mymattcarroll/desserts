// @flow

export function save(dessert: RequestReturnObject) {
  // Do nothing for now...
}

export function find(attr: string, what: RegExp) : RequestReturnObject[] {
  // Do nothing for now...
  return []
}
